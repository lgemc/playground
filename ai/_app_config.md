# App config

It will be an entire config section where each application could be
configured, in a pretty general way, it will be similar to env vars
in an operative system, some of them will be global some of them
scoped to the app.